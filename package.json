{
  "name": "cryptochain",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx ts-node-dev --respawn --transpile-only --poll index.ts",
    "test": "jest --watchAll"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^18.0.0",
    "jest": "^29.7.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  }
}
